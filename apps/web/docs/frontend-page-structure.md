# Next.js / shadcn-style Page Structure

- `/` — landing page with user/admin/login pathways
- `/register` — access request form
- `/login/user` — user login
- `/login/admin` — admin login
- `/auth/complete-2fa` — enrollment completion page
- `/dashboard` — user dashboard with credentials and access
  - `/dashboard/profile`
  - `/dashboard/datasets`
  - `/dashboard/access`
  - `/dashboard/reports`
- `/admin`
  - `/admin/registrations`
