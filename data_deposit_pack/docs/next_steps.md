# Recommended next build steps

1. Wire real auth + PBAC checks to deposit endpoints.
2. Replace placeholder workspace ID with actual workspace selector modal.
3. Add table view and richer metadata cards.
4. Generate sample rows and schema profiles during ingestion.
5. Enqueue pull jobs instead of creating direct pull request only.
6. Track lineage from deposit dataset -> workspace dataset -> analysis job -> report.
7. Add daily connector schedules and admin monitoring page.
8. Add dataset agreement / terms acceptance for restricted sources.
