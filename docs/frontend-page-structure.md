# Frontend Page Structure

The frontend application lives in `apps/web`.

- `app/dashboard` contains researcher dashboards and module pages.
- `app/admin` contains administration console pages.
- `components` contains reusable page and UI components.
- `src/lib/api` contains browser API clients.
