# Governance And RBAC

Administrative and SDOH governance routes use JWT authentication and role checks. Current SDOH governance access is available to `ADMIN`, `SUPER_ADMIN`, `ANALYST`, and `RESEARCHER`.
